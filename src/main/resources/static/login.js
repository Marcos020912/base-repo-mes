if (auth.token()) location.replace('index.html');
document.querySelector('#login-form').addEventListener('submit', async (event) => {
  event.preventDefault(); const form = new FormData(event.currentTarget); const message = document.querySelector('#login-message'); message.className = 'message'; message.textContent = 'Validando…';
  try { const response = await fetch('/api/v1/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ username: form.get('username'), password: form.get('password') }) }); const body = await response.json(); if (!response.ok) { if (body.code === 'EMAIL_NOT_VERIFIED') { location.assign(`verify.html?email=${encodeURIComponent(body.email || '')}`); return; } if (body.code === 'ACCOUNT_RESTRICTED') { message.className = 'message error'; message.innerHTML = 'Su cuenta fue restringida. Póngase en contacto con <a href="mailto:soporte@mes.gob.cu">soporte@mes.gob.cu</a>.'; return; } throw new Error(body.message || 'No se pudo iniciar sesión'); } localStorage.setItem('base-repo-token', body.token); localStorage.setItem('base-repo-user', JSON.stringify(body.user)); location.replace('index.html'); } catch (error) { message.className = 'message error'; message.textContent = error.message; }
});
