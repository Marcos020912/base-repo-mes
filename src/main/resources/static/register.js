if (auth.token()) location.replace('index.html');
document.querySelector('#register-form').addEventListener('submit', async (event) => { event.preventDefault(); const form=new FormData(event.currentTarget), message=document.querySelector('#register-message'); if(form.get('password')!==form.get('confirmation')){message.className='message error';message.textContent='Las contraseñas no coinciden.';return;} message.className='message';message.textContent='Creando cuenta…';try{const response=await fetch('/api/v1/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:form.get('username'),email:form.get('email'),password:form.get('password')})});const body=await response.json();if(!response.ok)throw new Error(body.message||'No se pudo crear la cuenta.');message.className='message success';message.textContent='Cuenta creada. Revise su correo para verificarla.';setTimeout(()=>location.assign(`verify.html?email=${encodeURIComponent(form.get('email'))}`),900)}catch(error){message.className='message error';message.textContent=error.message;}});
